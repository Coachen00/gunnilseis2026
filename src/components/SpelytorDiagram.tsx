import { cn } from "@/lib/utils";
interface SpelytorDiagramProps {
  className?: string;
}
const SpelytorDiagram = ({
  className
}: SpelytorDiagramProps) => {
  return;
};
export default SpelytorDiagram;